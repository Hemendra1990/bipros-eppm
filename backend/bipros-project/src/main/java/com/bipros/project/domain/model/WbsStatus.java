package com.bipros.project.domain.model;

/** IC-PMS execution status for a WBS element (M1 sheet). */
public enum WbsStatus {
    ACTIVE,
    IN_PROGRESS,
    NOT_STARTED,
    COMPLETED,
    DELAYED,
    AT_RISK
}
