package com.bipros.risk.domain.model;

/**
 * P6-style risk classification: every risk is either a Threat (negative)
 * or an Opportunity (positive).
 */
public enum RiskType {
    THREAT,
    OPPORTUNITY
}
